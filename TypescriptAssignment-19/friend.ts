let friends:string[] = ["Eman", "Bushra", "Murk", "Muqaddas", "Naseem",];
console.log(`I am inviting my ${friends.length} friends on tonight dinner.\n`);

for(let i = 0; i < friends.length; i++){
    console.log(`${i + 1}. ${friends[i]}`)
}
